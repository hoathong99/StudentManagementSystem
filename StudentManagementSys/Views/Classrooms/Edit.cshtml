@model StudentManagementSys.Views.Classrooms.ClassroomVM

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Classroom</h4>
<hr />
<div>
    <a asp-action="StudentRemovalTable" asp-route-id="@Model.CRID">To Students table</a>
</div>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CRID" />
            <div class="form-group">
                <label asp-for="HomeRoomTeacherID" class="control-label"></label>
                <input asp-for="HomeRoomTeacherID" class="form-control" />
                <span asp-validation-for="HomeRoomTeacherID" class="text-danger"></span>
            </div>
            @*<div class="form-group">
                <label asp-for="MonitorID" class="control-label"></label>
                <input asp-for="MonitorID" class="form-control" />
                <span asp-validation-for="MonitorID" class="text-danger"></span>
            </div>*@
            <label asp-for="MonitorID" class="control-label"></label>
            <select asp-for="MonitorID" asp-items="@Model.StudentList" class="form-control" aria-required="true">
                <option disabled selected>Select monitor</option>
            </select>
            @*<label> Student list</label>
            <table class="table">
                <thead>
                    <tr>
                        <th>
                            Name
                        </th>
                        <th>
                            ID
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.StudentsID)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.id)
                            </td>
                            <td>
                                <a asp-action="RemoveFromClass" asp-route-sid="@item.id" asp-route-cid="@Model.CRID">Edit</a> |
                            </td>
                        </tr>
                    }
                </tbody>
            </table>*@
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
